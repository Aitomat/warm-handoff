# Skill-Welle A4 — warm-handoff

Stand: 30.08.2026, 02:33. Branch `main`, Tip vorher `993f70d`.

## Was gemacht wurde

1. **`references/historie.md` auf Englisch** (1.981 Zeilen). Fließtext,
   Überschriften und Tabellen englisch; alle 53 datierten Yasin-Zitate
   wörtlich deutsch, ebenso Banner, Handoff-Vorlagen und
   `>>>Userantwort:`-Blöcke (sie werden wörtlich ins deutschsprachige
   Handoff geschrieben). Deutsche Fachbegriffe beim ersten Auftreten in
   Klammern erklärt. SKILL.md war bereits englisch — ergänzt wurde nur ein
   Glossar vor der Handoff-Struktur.
2. **Welle 25 als Evidenz** in SKILL.md-Tabelle und `docs/sitzungen-bilanz.md`:
   3 Themen-Wächter + Merge-Wächter, 15 Arbeiter, 29 Anfragen, 1.192k,
   5 Fertigmeldungen, 64 min, kein Absturz. Lesart: günstigste UND
   schnellste Struktur bisher — löst den Zielkonflikt aus Welle 23 auf.
3. **Wächter-Anzahl** beantwortet: einer je disjunktem Thema mit 4–6
   Aufträgen; unter 4 lohnt `start_ctx × 2` nicht, über 6 wird der Wächter
   zum Engpass. Mit W23/24/25 belegt.
4. **Gedächtnis-Block** (Langzeit/Kurzzeit) als Pflichtabschnitt in
   Handoff-Punkt 7, Vorlage aus Handoff E.
5. **`scripts/ctx.sh`**: erste Anfrage einer Sitzung → „Start-Cacheaufbau
   (einmalig)" statt „Neues gespeichert", plus Hinweis, dass Cache-Write
   ≠ aktueller Kontext. Gegen die laufende Session getestet, greift.

## Offen

`SKILL.de.md` (deutscher Zwilling) und `README.md` wurden nicht
nachgeführt — Welle 25, Wächter-Anzahl und Gedächtnis-Block fehlen dort.
